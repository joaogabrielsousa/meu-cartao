const btnMenu = document.getElementById('btnMenu')
const itensMenu = document.getElementById('itensMenu')

btnMenu.addEventListener('click', () => {
    itensMenu.classList.toggle('menu-off')
});
    
